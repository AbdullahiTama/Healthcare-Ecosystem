---
scope: HealthCare-Ecosystem Super Admin + whole ecosystem
purpose: Owner command center that scales to 1000+ businesses without drift
altitude: feature
updated: 2026-09-06T21:15
---

- (decision) AD-1 Repository seam only data gate — UI->repository->sbFetch, two adapters; Binds all, Prevents unscoped writes
- (decision) AD-2 RLS tenant boundary behaviorally verified — business_id IN current_business_ids() OR is_platform_admin; Binds all
- (decision) AD-3 Financial SECURITY DEFINER idempotent — calculate_agent_earnings + mark_payout_paid ON CONFLICT, Binds CAP-5,7
- (decision) AD-4 Dark-first Quiet Chrome tokens — CSS vars, color=state, 36px rows, sheet not modal; Binds CAP-3,6
- (decision) AD-5 Permission one checklist two matrices — reuse navCatalogueFor for business+platform; Binds CAP-4
- (decision) AD-6 Module registry single source — MODULES.types gates, NAV_ORDER orders; Binds CAP-1,2,4
- (decision) AD-7 State predictable no global cache — useState+Context only, offline queue exception; Binds all
- (decision) AD-8 Supabase platform Vercel per app env config — anon via VITE_, service-role only in api; Binds all
- (decision) AD-9 Migrations tracked verified by re-read — CLI supabase/migrations + pg_policies probe; Binds all
- (decision) AD-10 Command palette power path table as product — Cmd+K cmdk, dense table primary; Binds CAP-1,2
- (event) spine finalized — 10 ADs, 0 lint findings, paradigm Repository-Seam+RLS+Quiet Chrome, scope feature, status final
