---
topic: UX review & premium plan
updated: 2026-08-16T09:30
---

- (decision) User (Joe) requested a UX review of both apps (CareHub + CareFind) to identify improvements for premium SaaS positioning. Chose Prioritized Improvement Plan as outcome (not spine authoring, not full validate report, not Stitch handoff). Stakes: pre-launch / finding product-market fit. Direction set by user.
- (decision) Audit findings (code, not docs): both apps are inline-style-driven with a real token system (theme.js) that is broadly adopted but drifting. Key gaps: (1) theme.type/space/motion near-dead tokens, hand-typed px everywhere; (2) CareFind declares Geist font but never ships it (only Lora woff2 present) - renders system fonts; (3) foreign palettes (slate #0f172a, amber #fcd34d/#92400e, star-amber #f5b301) mixed into teal/warm-gray; (4) ErrorState defined but used nowhere in either app - catch(e){} swallows read errors; (5) Skeleton/CardSkeleton unused; (6) CareFind PostCard.jsx dead code - Feed.jsx inlines 330-line card; (7) monoliths: Feed 2655, AdminPanel 1887, LiveActivity 1097, POS 883; (8) Register.jsx/Onboarding.jsx are the weakest visual links; (9) duplicated StatTile x2, StatusBadge x2, mobile app bar x2; (10) Landing + BusinessProfile are genuinely premium anchors.
- (event) Wrote UX_IMPROVEMENT_PLAN.md: 4 tiers (T1 foundational polish, T2 acquisition surfaces, T3 enterprise trust/density, T4 structural), 5 premium tells to eliminate, what-not-to-do per BRAND_GUIDELINES, 4-week execution order, 3 open questions. Path: _bmad-output/planning-artifacts/ux-designs/ux-HealthCare-Ecosystem-2026-08-15/UX_IMPROVEMENT_PLAN.md
- (event) T1.1 completed: shipped Geist fonts. Downloaded geist@1.7.2 npm tarball (GitHub raw fetch aborted - used registry), copied variable woff2 (geist-var.woff2, geist-mono-var.woff2) + OFL-LICENSE.txt to both apps/public/fonts (sans 69,652 / mono 71,368 / license 4,368 bytes). Added @font-face (weight 100 900, swap) to both global.css; CareHub global.css + theme.js + index.html now use Geist stacks; CareFind theme already declared Geist.
- (event) T1.2 completed: added semantic tokens + re-pointed ad-hoc hex literals in both apps. CareFind theme: starAmber, amberText/Bg/Soft/Border, slate, slateMuted, greenLive, dangerBorder, alertLight. CareHub theme: amberText, amberBorder, amberDeep, slate, dangerBorder, textFaint. Re-pointed offenders in Feed, LiveSession, LiveDashboard, GiftPanel, News, AdminPanel, AdminLogin, BusinessProfile, DrugProfile, LiveShow, ProfessionalMonetization, ArticleEditor, PlaylistView, Profile, VerifyProfessional, DesktopHeader, VoiceRecorder, Card, BusinessDashboard, ClaimBusiness, ClaimStaffPosition, ui/index (CareFind) + Register, AdminDashboard, AdminReferralPanels, Inventory, Login, AgentLogin, POS (CareHub). Left intentional content colors: pen/annotation color-picker defaults (ArticleEditor, LiveSession). Tests green: CareFind 302, CareHub 306. Remaining literal sweep: pure-white/system-token hex (#fff, #f9f9f9, #f0f0f0, #e5e7eb in Register) deferred to T2 re-skin of Register/AdminDashboard.
- (event) T1.3 completed: wired shared ErrorState into CareHub modules that silently swallowed read failures. Pattern: added loadError state, set in catch + cleared on success, render `{loading ? <Loading /> : loadError ? <ErrorState onRetry={load} /> : empty ? <Empty/> : data}`. Applied to RxInbox, Reception, Triage, Doctor, Lab, Appointments, Demand (BusinessDashboard.loadProducts already falls back to cache — left as-is). CareFind: News.loadNews now throws+sets loadError (was ignoring supabase error entirely) with retry; Onboarding.loadProfile now surfaces load errors with a profileLoadKey retry (was silently swallowing). Deviation: Login/AdminLogin keep inline auth-submission paragraphs — ErrorState is a full-view load-failure panel; a "wrong password" message must stay inline next to the form, so it was not applied there. Tests green (CareHub 306, CareFind 302).
- (event) T1.4 completed: replaced spinner/plain-text loading with 3× CardSkeleton stacks in CareHub Orders, Stock, Appointments and CareFind News, Wallet, BusinessProfile. Removed now-unused Loading imports from Orders, Appointments, News, Wallet, BusinessProfile. Inventory has loading state but renders no loading indicator — skipped (nothing to replace). Tests green (CareFind 302, CareHub 306).
- (event) T1.5 completed: deleted dead `apps/carefind/src/modules/social-feed/components/PostCard.jsx` — grep confirmed zero imports (its `navigate` calls also lacked a useNavigate import, i.e. latent bug). Feed.jsx's 330-line inline card is deeply coupled to Feed's own state/helpers; extraction is deferred to T4.4 as the plan recommends. No test impact.
- (event) T1.6 completed: consolidated notification badge border. DesktopHeader.jsx:91 used `cardBg` on a `gray50` button surface (mismatch); Feed.jsx:1484 used `navy` on the navy mobile app bar (correct). Fixed DesktopHeader to `gray50` — the badge border is a cutout ring that must match the surface it sits on, so the two values legitimately differ. Tests green (CareFind 302).
- (event) T1.7 completed: standardized radius literals in CareFind Search.jsx, Onboarding.jsx, Feed.jsx, and the DesktopHeader badge (paired with T1.6). Mapped 13/11/12/9→theme.radius.md, 16→theme.radius.lg, 8→theme.radius.sm. Also tokenized remaining ad-hoc hexes in Feed.jsx: added dangerGradient token (#7F1D1D→#B91C1C live banner), #a16207→amberText (unclaimed review text), #ccc→gray300 (inactive star), #666→textLight (login prompt), #B91C1C→danger (Watch pill). Remaining #fff (text-on-brand) + #fde68a (pen highlight default) intentionally literal; #10a hits are comment markers. Out-of-scope files (CommentThread, GoLive, Stories, DrugProfile, etc.) still carry radius literals — tracked for later tiers. Tests green: CareFind 302, CareHub 306.

- (event) T2.0 critical fix: corruption quarantine. Post-T1 tests passed but a fresh corruption scan surfaced `b->o` mangling + UTF-8 BOM in exactly two files at HEAD: DesktopHeader.jsx and Onboarding.jsx (match any of `supaoase|oorderRadius|outton|availaole|oackground`). Root cause traced to commit 64373cb (Tier 1) - likely PowerShell `Set-Content -Encoding UTF8` writes during T1 edits; pre-session commit c08a427 had both files clean. Current HEAD (e01d560) had since gained 2 commits (fail-loud dispensing + docs). Fixed DesktopHeader.jsx byte-safely: restored clean blob from c08a427 via `cmd /c git show` (avoids PowerShell encoding mangling) then re-applied intended T1.6/T1.7 edits (borderRadius 8 -> theme.radius.sm, badge border cardBg -> gray50). Onboarding.jsx handled by full rewrite (T2.3). Lesson: any PowerShell byte write that isnt `[System.IO.File]::WriteAllBytes` (or a `cmd /c` redirect) can corrupt; the Write/Edit tools also emit BOMs that must be stripped byte-level. Verified: all edited files BOM-free (except two CareHub files whose BOMs pre-existed in HEAD), corruption scan 0.
- (event) T2.1 completed: re-skinned CareHub Register.jsx toward tokens + shared primitives. Swapped remaining ad-hoc hexes (#FDFBF7, #e5e7eb, #0E6F5A) for theme tokens (tealMist, border, tealDeep, cardBg), padding 12px -> theme.space[6], borderRadius 10px -> theme.radius.md. Only #fff (text-on-brand) remains literal. Tests green (CareHub 312).
- (event) T2.2 completed: re-skinned CareHub AdminDashboard.jsx to zero non-white hexes. Replaced #f9fafb -> theme.bg, #d97706 -> theme.warning, #0E6F5A -> theme.tealDeep, #f3f4f6 -> theme.gray100, #666 -> theme.gray600, #aaa -> theme.textLight, #ccc -> theme.gray400, #f9f9f9 -> theme.hairline; system-ui stack -> theme.fontFamily; borderRadius 10px -> theme.radius.md. Tests green (CareHub 312).
- (event) T2.3 completed: rewrote CareFind Onboarding.jsx. Killed the lingering corruption (supabase import), unified the duplicated mobile/desktop forms into ONE formBody shared by both shells (mirrors Login authForm pattern), adopted shared Inp/TealBtn primitives, cards -> theme.radius.xl, kept the debounced live username availability check (450ms, maybeSingle, self-match allowed) and preserved T1.3's ErrorState/profileLoadKey retry. Critically fixed a hooks-order violation T1.3 had introduced: the username-check useEffect sat AFTER conditional return <Loading>/<ErrorState> (and a duplicated Loading check). Tests green (CareFind 302).
- (event) T2.4 completed: de-duplicated CareFind login font stack. Login.jsx:170 + :193 both hardcoded `system-ui, -apple-system, sans-serif`; replaced with theme.fontFamily (single source = '"Geist", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif'). Zero system-ui occurrences remain in Login.jsx. Tests green (CareFind 302).
- (event) T2.5 completed: added the empty-state illustration language to onboarding/auth. Added BrandArt to CareFind ui/index.jsx: a reusable geometric brand-palette SVG (rounded rects + head/shoulders mark) matching BRAND_GUIDELINES' "simple, geometric, brand-palette, empty states + onboarding only" rule. `tone` picks ink: 'light' on navy/teal hero surfaces (Onboarding mobile header, size 72), 'dark' on light surfaces (desktop centered card, size 96). Stripped BOM the Edit tool added to ui/index.jsx. Tests green (CareFind 302).
- (event) T2.6 completed: consolidated marketing pages' third visual language (ForBusiness.jsx/About.jsx). Both already used theme.fontFamily/fontDisplay + destructured tokens, and About had zero non-white hexes. The only palette drift: ForBusiness FEATURES array hardcoded brand hexes #0E6F5A/#1A8A72/#155A4B/#0B4A3E -> replaced with theme.tealDeep/tealBright/navySoft/navy (already destructured at top of file). The scroll-animated hero + clamp() type are the marketing pages' deliberate bespoke system (per BRAND_GUIDELINES hero exception), so left intact; remaining #fff / rgba(255,255,255,*) are intentional. Final acceptance scan: acquisition surfaces (ForBusiness, About, Login, Onboarding, CareHub Register) have zero non-white ad-hoc hexes and zero ad-hoc font stacks. Tests green: CareFind 302, CareHub 312.
- (event) Tier 2 acceptance verified: acquisition screens use shared primitives (Inp/TealBtn/ErrorState/Loading/BrandArt/Card) + token values only; a visitor moving Landing (ForBusiness) -> Login -> Onboarding sees one continuous brand (teal/navy palette, Geist, xl radii, shared illustration language). All 7 working-tree files verified BOM/corruption-clean. Todo list fully green for Tier 2.